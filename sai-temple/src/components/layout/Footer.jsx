/**
 * Footer (placeholder)
 * Full footer UI (links, contact info, social, newsletter, etc.) will be
 * built in a later task. This placeholder only reserves layout space and
 * keeps routing/structure functional.
 */
function Footer() {
  return (
    <footer className="w-full border-t border-primary/20 bg-background">
      <div className="mx-auto w-full max-w-7xl px-4 py-8 sm:px-6 lg:px-8">
        {/* Footer content placeholder — to be implemented in a future task */}
      </div>
    </footer>
  );
}

export default Footer;
