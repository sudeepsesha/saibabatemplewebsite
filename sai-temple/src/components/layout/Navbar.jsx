/**
 * Navbar (placeholder)
 * Full navigation UI (logo, links, mobile menu, animations) will be built
 * in a later task. This placeholder only reserves layout space and keeps
 * routing/structure functional.
 */
function Navbar() {
  return (
    <header className="w-full border-b border-primary/20 bg-background">
      <nav className="mx-auto flex h-16 w-full max-w-7xl items-center px-4 sm:px-6 lg:px-8">
        {/* Navbar content placeholder — to be implemented in a future task */}
      </nav>
    </header>
  );
}

export default Navbar;
